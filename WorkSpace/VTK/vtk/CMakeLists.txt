cmake_minimum_required(VERSION 2.8)
project(VTKInstall)
find_package(VTK REQUIRED)
include(${VTK_USE_FILE})
add_executable(${PROJECT_NAME} VTKInstall.cpp)
target_link_libraries(${PROJECT_NAME} vtkRendering vtkCommon)
